{
  "name": "hl7-mapper",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Mapper de mensajes HL7 v2.x: parsing, analisis de integridad (requeridos/opcionales/faltantes) y descarga JSON. Sin IA.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "hl7-dictionary": "^1.0.0",
    "sql.js": "^1.14.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
